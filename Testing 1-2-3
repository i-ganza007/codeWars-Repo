var number=function(array){
  if(array.length == 0){
    return array;
  }
  else{
    let result = array.map((x,idx)=>`${idx+1}: ${x}`)
    return result
  }
}

//refactored code
var number=function(array){
  return array.length == 0 ? array : array.map((x,idx)=>`${idx+1}: ${x}`)
}
